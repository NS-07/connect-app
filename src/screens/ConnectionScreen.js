import React from "react";
import { View, StyleSheet, Text } from "react-native";

const ConnectionScrren = () => {
  return <Text>Connections Screen</Text>;
};

const styles = StyleSheet.create({});

export default ConnectionScrren;
